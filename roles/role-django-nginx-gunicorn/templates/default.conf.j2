server {
    listen 80;
    server_name 10.0.0.111;

    location / {
        proxy_pass http://unix:/run/gunicorn.sock;
    }

    location = /favicon.ico {
        access_log off;
        log_not_found off;
    }

    location /static/ {
        root /var/www/portal/;
    }
}